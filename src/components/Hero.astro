<section class="hero">
    <div class="hero-eyebrow">Available for learning more</div>
    <div class="hero-name">Haidar Ali Fawwaz Nasirodin</div>
    <div class="hero-terminal mono" id="terminal">
        <span id="typed"></span><span class="cursor"></span>
    </div>
    <p class="hero-desc">
        I'm a student who is interested to pursue Software/AI Engineering.
        Planning to take CS in University. I love to build systems that can
        genuinely help people behind the scenes, not just fancy appearance.
    </p>
    <div class="badge-row">
        <span class="badge badge-go">Go</span>
        <span class="badge badge-rust">Rust</span>
        <span class="badge badge-py">Python</span>
        <span class="badge badge-ts">TypeScript</span>
        <span class="badge badge-cpp">C++</span>
    </div>
    <div class="hero-btns">
        <a class="btn-primary" href="https://github.com/Dadarzz2405" target="_blank" rel="noopener noreferrer">View on GitHub</a>
    </div>
</section>

<style>
    .hero {
        padding: 60px 28px 40px;
        border-bottom: 1px solid #21262d;
    }
    .hero-eyebrow {
        font-family: "JetBrains Mono", monospace;
        font-size: 11px;
        color: #7ee787;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        margin-bottom: 20px;
    }
    .hero-name {
        font-family: "JetBrains Mono", monospace;
        font-size: 38px;
        font-weight: 700;
        line-height: 1.15;
        margin-bottom: 8px;
        color: #f0f6fc;
    }
    .hero-terminal {
        font-family: "JetBrains Mono", monospace;
        font-size: 16px;
        color: #8b949e;
        margin-bottom: 28px;
        min-height: 22px;
    }
    .cursor {
        display: inline-block;
        width: 9px;
        height: 16px;
        background: #7ee787;
        margin-left: 2px;
        vertical-align: middle;
        animation: blink 1s step-end infinite;
    }
    .hero-desc {
        font-size: 14px;
        color: #8b949e;
        line-height: 1.75;
        max-width: 480px;
        margin-bottom: 32px;
    }
    .badge-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 32px;
    }
    .badge {
        font-family: "JetBrains Mono", monospace;
        font-size: 11px;
        padding: 4px 10px;
        border-radius: 4px;
        letter-spacing: 0.04em;
    }
    .badge-go {
        background: #00acd730;
        color: #00acd7;
        border: 1px solid #00acd740;
    }
    .badge-rust {
        background: #ce422b30;
        color: #ff8552;
        border: 1px solid #ce422b40;
    }
    .badge-py {
        background: #3776ab30;
        color: #60a5fa;
        border: 1px solid #3776ab40;
    }
    .badge-ts {
        background: #3178c630;
        color: #7ec8e3;
        border: 1px solid #3178c640;
    }
    .badge-cpp {
        background: #6e40c920;
        color: #a78bfa;
        border: 1px solid #6e40c940;
    }
    .hero-btns {
        display: flex;
        gap: 12px;
    }
    .btn-primary {
        font-family: "JetBrains Mono", monospace;
        font-size: 12px;
        padding: 8px 18px;
        background: #238636;
        color: #f0f6fc;
        border: 1px solid #2ea043;
        border-radius: 6px;
        cursor: pointer;
        text-decoration: none;
    }
    .btn-outline {
        font-family: "JetBrains Mono", monospace;
        font-size: 12px;
        padding: 8px 18px;
        background: transparent;
        color: #8b949e;
        border: 1px solid #30363d;
        border-radius: 6px;
        cursor: pointer;
        text-decoration: none;
    }
</style>

<script>
    const phrases = [
        "building things that scale.",
        "writing Go that ships.",
        "thinking in systems.",
        "exploring AI pipelines.",
    ];
    let pi = 0,
        ci = 0,
        del = false;
    const el = document.getElementById("typed");
    function type() {
        const phrase = phrases[pi];
        if (!del) {
            el.textContent = phrase.slice(0, ++ci);
            if (ci === phrase.length) {
                del = true;
                setTimeout(type, 1800);
                return;
            }
            setTimeout(type, 60);
        } else {
            el.textContent = phrase.slice(0, --ci);
            if (ci === 0) {
                del = false;
                pi = (pi + 1) % phrases.length;
                setTimeout(type, 300);
                return;
            }
            setTimeout(type, 30);
        }
    }
    type();
</script>
